specVersion: "v1"
serviceId: "c"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "c-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
